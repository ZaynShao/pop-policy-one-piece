import { StubCard } from '@/components/StubCard';

export function TicketsPage() {
  return (
    <StubCard
      title="工单处理"
      desc="sys_admin · 数据校正 / 归属转移"
      refLine="UI-LAYOUT-V1 §3.3 R2-③ / PRD §3.8 H 模块"
    />
  );
}
