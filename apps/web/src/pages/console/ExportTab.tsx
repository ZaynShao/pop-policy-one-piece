import { StubCard } from '@/components/StubCard';

export function ExportTab() {
  return (
    <StubCard
      title="导出中心"
      desc="lead / pmo · I 模块 · 触发截图 / PDF → ExportRecord"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §3.x I 模块"
    />
  );
}
