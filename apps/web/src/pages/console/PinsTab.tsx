import { StubCard } from '@/components/StubCard';

export function PinsTab() {
  return (
    <StubCard
      title="图钉清单"
      desc="lead / pmo · B7 后台维护视图 · 单条详情 → 留言板 / 状态变更"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §3.3 B 模块"
    />
  );
}
