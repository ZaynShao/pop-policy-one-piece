import { StubCard } from '@/components/StubCard';

export function MyConsumptionTab() {
  return (
    <StubCard
      title="我的消费记录"
      desc="local_ga 个人视角 · 自己消费的工具记录"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §1.3"
    />
  );
}
