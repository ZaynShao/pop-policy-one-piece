import { StubCard } from '@/components/StubCard';

export function ThemesTab() {
  return (
    <StubCard
      title="政策主题管理"
      desc="central_ga 主用 · C1-C6 · 创建 / 拉取覆盖清单 / 发布 / 归档"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §3.4 C 模块"
    />
  );
}
