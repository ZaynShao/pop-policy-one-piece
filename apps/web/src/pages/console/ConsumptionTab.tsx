import { StubCard } from '@/components/StubCard';

export function ConsumptionTab() {
  return (
    <StubCard
      title="消费排行"
      desc="D17 + 1.4 · 按角色过滤可见范围:lead / pmo = 全员;central_ga = 自己工具"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §3.5 D 模块"
    />
  );
}
