import { StubCard } from '@/components/StubCard';

export function OrgsTab() {
  return (
    <StubCard
      title="机构列表(关系档案)"
      desc="K1 / K5 · 全角色 R / local_ga + central_ga 可 CUself · 单条详情含历史 Visit + 该机构关键人列表"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §4.3.6 K 模块"
    />
  );
}
