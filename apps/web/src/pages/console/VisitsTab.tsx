import { StubCard } from '@/components/StubCard';

export function VisitsTab() {
  return (
    <StubCard
      title="拜访清单"
      desc="local_ga 主用 · B14 筛选 + B11 单条编辑 + B12 H5 入口(PRD §4.3)"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §4.3"
    />
  );
}
