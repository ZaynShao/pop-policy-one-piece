import { StubCard } from '@/components/StubCard';

export function ContactsTab() {
  return (
    <StubCard
      title="关键人列表(关系档案)"
      desc="K2 / K5 · 全角色 R / local_ga + central_ga 可 CUself · 单条详情含历史 Visit + 所属机构跳转"
      refLine="UI-LAYOUT-V1 §3.2 R2-② / PRD §4.3.6 K 模块"
    />
  );
}
